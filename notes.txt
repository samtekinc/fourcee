
 - module service
assign module to OU


 - orchestration service
 trigger an execution of a module propagation
  - pull list of downstream OUs from propagation
  - extract all associated accounts from OUs
  - extract list of all accounts with active tf states for the module propagation
  - for all new accounts, create a tf state entry and execute the terraform
  - for all existing accounts which still have propagation, execute the terraform
  - for all existing accounts which are no longer in propagation, execute a terraform destroy